{
  "name": "clinical-data-reconciliation-engine",
  "private": true,
  "scripts": {
    "start": "npm start --prefix backend",
    "heroku-postbuild": "npm install --include=dev --prefix backend && npm run build --prefix backend"
  },
  "engines": {
    "node": "20.x"
  }
}
