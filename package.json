{
  "name": "clinical-data-reconciliation-engine",
  "private": true,
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm install --include=dev --prefix backend && rm -f backend/tsconfig.build.tsbuildinfo && npm run build --prefix backend && ls backend/dist"
  },
  "engines": {
    "node": "20.x"
  }
}
