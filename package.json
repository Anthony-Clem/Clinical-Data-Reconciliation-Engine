{
  "name": "clinical-data-reconciliation-engine",
  "private": true,
  "scripts": {
    "start": "npm start --prefix backend",
    "heroku-postbuild": "npm install --prefix backend && npm run build --prefix backend"
  }
}
